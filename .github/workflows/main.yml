name: Build Bharat AI Pilot APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

env:
  # Force GitHub to bypass version deprecation rules
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout source code
      uses: actions/checkout@v4

    - name: Set up Java Development Kit
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: Smart Code Repair Tool
      run: |
        # Clear out previous surface-level workspace locks
        rm -f build.gradle settings.gradle

        # Re-build clean root connection tracks
        cat << 'EOF' > settings.gradle
        pluginManagement {
            repositories {
                google()
                mavenCentral()
                gradlePluginPortal()
            }
        }
        dependencyResolutionManagement {
            repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
            repositories {
                google()
                mavenCentral()
            }
        }
        rootProject.name = "BharatPilot"
        include ':app'
        EOF

        # Re-build clean root application rules
        cat << 'EOF' > build.gradle
        plugins {
            id 'com.android.application' version '8.2.0' apply false
            id 'org.jetbrains.kotlin.android' version '1.9.0' apply false
        }
        EOF

        # Guarantee full operational folder structures
        mkdir -p app/src/main/java/com/bharatpilot/app
        mkdir -p app/src/main/res/xml
        mkdir -p app/src/main/res/values

        # Relocate user files safely if they are sitting on the surface layout
        mv -f MainActivity.kt app/src/main/java/com/bharatpilot/app/MainActivity.kt || true
        mv -f VoiceAutomationService.kt app/src/main/java/com/bharatpilot/app/VoiceAutomationService.kt || true
        mv -f AndroidManifest.xml app/src/main/AndroidManifest.xml || true
        mv -f AppConfig.kt app/src/main/java/com/bharatpilot/app/AppConfig.kt || true
        mv -f BharatPilotApplication.kt app/src/main/java/com/bharatpilot/app/BharatPilotApplication.kt || true

        # Re-compile app build targets cleanly
        cat << 'EOF' > app/build.gradle
        plugins {
            id 'com.android.application'
            id 'org.jetbrains.kotlin.android'
        }

        android {
            namespace 'com.bharatpilot.app'
            compileSdk 34

            defaultConfig {
                applicationId "com.bharatpilot.app"
                minSdk 26
                targetSdk 34
                versionCode 1
                versionName "1.0"
            }

            buildTypes {
                release {
                    minifyEnabled false
                    proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
                }
            }
            compileOptions {
                sourceCompatibility JavaVersion.VERSION_1_8
                targetCompatibility JavaVersion.VERSION_1_8
            }
            kotlinOptions {
                jvmTarget = '1.8'
            }
        }

        dependencies {
            implementation 'androidx.core:core-ktx:1.12.0'
            implementation 'androidx.appcompat:appcompat:1.6.1'
            implementation 'com.google.android.material:material:1.10.0'
        }
        EOF

        # Re-write resource layout strings
        cat << 'EOF' > app/src/main/res/values/strings.xml
        <resources>
            <string name="app_name">Bharat Pilot</string>
            <string name="accessibility_description">Bharat Pilot runs actions inside device memory. No data leaves the terminal wrapper.</string>
        </resources>
        EOF

        # Re-write configuration setup bounds
        cat << 'EOF' > app/src/main/res/xml/accessibility_service_config.xml
        <?xml version="1.0" encoding="utf-8"?>
        <accessibility-service xmlns:android="http://android.com"
            android:accessibilityEventTypes="typeAllMask"
            android:accessibilityFeedbackType="feedbackGeneric"
            android:accessibilityFlags="flagDefault|flagRetrieveInteractiveWindows|flagReportViewIds"
            android:canRetrieveWindowContent="true"
            android:canPerformGestures="true"
            android:description="@string/accessibility_description" />
        EOF

    - name: Set up Setup-Gradle Automation Action
      uses: gradle/actions/setup-gradle@v4
      with:
        gradle-version: 8.2

    - name: Compile and Build Android APK File
      run: gradle :app:assembleDebug --no-daemon

    - name: Upload Finished App Installer
      uses: actions/upload-artifact@v4
      with:
        name: Bharat-AI-Pilot-App
        path: "app/build/outputs/apk/debug/*.apk"
